import jwt from "jsonwebtoken";import {AppError} from "./errors.js";
type GoogleClaims={sub:string;email:string;email_verified:boolean;name?:string;picture?:string;aud:string;iss:string;exp:number};let cached:{expiresAt:number;keys:Record<string,string>}|null=null;
async function googleKeys(){if(cached&&cached.expiresAt>Date.now())return cached.keys;const response=await fetch("https://www.googleapis.com/oauth2/v1/certs");if(!response.ok)throw new AppError(503,"GOOGLE_AUTH_UNAVAILABLE","Google sign-in is temporarily unavailable");const maxAge=Number(response.headers.get("cache-control")?.match(/max-age=(\d+)/)?.[1]||300);cached={keys:await response.json() as Record<string,string>,expiresAt:Date.now()+maxAge*1000};return cached.keys}
export async function verifyGoogleIdToken(idToken:string,clientId:string):Promise<GoogleClaims>{const decoded=jwt.decode(idToken,{complete:true});const kid=decoded&&typeof decoded==="object"&&"header" in decoded?decoded.header.kid:undefined;if(!kid)throw new AppError(401,"INVALID_GOOGLE_TOKEN","Google credential is invalid");const certificate=(await googleKeys())[kid];if(!certificate)throw new AppError(401,"INVALID_GOOGLE_TOKEN","Google credential uses an unknown signing key");try{const claims=jwt.verify(idToken,certificate,{algorithms:["RS256"],audience:clientId,issuer:["accounts.google.com","https://accounts.google.com"]}) as GoogleClaims;if(!claims.sub||!claims.email||!claims.email_verified)throw new Error("Unverified email");return claims}catch{throw new AppError(401,"INVALID_GOOGLE_TOKEN","Google credential could not be verified")}}
