openapi: 3.1.0
info:
  title: Aster & Row Commerce API
  version: 1.0.0
servers:
  - url: http://localhost:4000
paths:
  /health:
    get:
      summary: Service health
      responses: { "200": { description: Healthy } }
  /api/v1/auth/register:
    post:
      summary: Register customer
      requestBody:
        {
          required: true,
          content:
            {
              application/json:
                { schema: { $ref: "#/components/schemas/Register" } },
            },
        }
      responses:
        {
          "201": { description: Created },
          "400": { $ref: "#/components/responses/Error" },
        }
  /api/v1/auth/login:
    post:
      summary: Login and set rotating refresh cookie
      requestBody:
        {
          required: true,
          content:
            {
              application/json:
                { schema: { $ref: "#/components/schemas/Credentials" } },
            },
        }
      responses:
        {
          "200": { description: Access token issued },
          "401": { $ref: "#/components/responses/Error" },
        }
  /api/v1/auth/refresh:
    post:
      summary: Rotate refresh session
      responses:
        {
          "200": { description: Access token issued },
          "401": { $ref: "#/components/responses/Error" },
        }
  /api/v1/auth/logout:
    post:
      summary: Revoke refresh session
      responses: { "200": { description: Logged out } }
  /api/v1/products:
    get:
      summary: Search active products and variants
      parameters:
        - { in: query, name: q, schema: { type: string } }
        - { in: query, name: category, schema: { type: string } }
      responses: { "200": { description: Product list } }
  /api/v1/products/{id}:
    get:
      summary: Product details
      parameters: [{ $ref: "#/components/parameters/Id" }]
      responses:
        {
          "200": { description: Product },
          "404": { $ref: "#/components/responses/Error" },
        }
  /api/v1/account/reviews:
    get:
      summary: List the signed-in customer's reviews and moderation status
      security: [{ bearerAuth: [] }]
      responses:
        {
          "200": { description: Customer reviews },
          "401": { $ref: "#/components/responses/Error" },
        }
    post:
      summary: Submit one product review for moderation
      security: [{ bearerAuth: [] }]
      responses:
        {
          "201": { description: Review submitted },
          "409": { $ref: "#/components/responses/Error" },
        }
  /api/v1/account/payments:
    get:
      summary: List payment attempts, gateway references and refunds owned by the signed-in customer
      security: [{ bearerAuth: [] }]
      responses:
        {
          "200": { description: Customer payment history },
          "401": { $ref: "#/components/responses/Error" },
        }
  /api/v1/cart:
    get:
      summary: Read customer or guest cart
      responses: { "200": { description: Cart } }
  /api/v1/cart/items:
    put:
      summary: Add, update, or remove a cart line
      requestBody:
        {
          required: true,
          content:
            {
              application/json:
                {
                  schema:
                    {
                      type: object,
                      required: [variantId, quantity],
                      properties:
                        {
                          variantId: { type: string, format: uuid },
                          quantity: { type: integer, minimum: 0, maximum: 20 },
                        },
                    },
                },
            },
        }
      responses:
        {
          "200": { description: Cart updated },
          "409": { $ref: "#/components/responses/Error" },
        }
  /api/v1/checkout/quote:
    post:
      summary: Calculate authoritative prices and available delivery services
      requestBody:
        {
          required: true,
          content:
            {
              application/json:
                { schema: { $ref: "#/components/schemas/CheckoutQuote" } },
            },
        }
      responses:
        {
          "200": { description: Checkout quote calculated },
          "409": { $ref: "#/components/responses/Error" },
          "422": { $ref: "#/components/responses/Error" },
        }
  /api/v1/checkout:
    post:
      summary: Price, reserve inventory, and create an order idempotently
      parameters:
        [
          {
            in: header,
            name: Idempotency-Key,
            required: true,
            schema: { type: string, minLength: 8, maxLength: 100 },
          },
        ]
      requestBody:
        {
          required: true,
          content:
            {
              application/json:
                { schema: { $ref: "#/components/schemas/Checkout" } },
            },
        }
      responses:
        {
          "201": { description: Order created },
          "409": { $ref: "#/components/responses/Error" },
          "422": { $ref: "#/components/responses/Error" },
        }
  /api/v1/orders/{number}/track:
    get:
      summary: Track an order
      parameters:
        [{ in: path, name: number, required: true, schema: { type: string } }]
      responses:
        {
          "200": { description: Tracking history },
          "404": { $ref: "#/components/responses/Error" },
        }
  /api/v1/admin/products:
    post:
      summary: Create product with variants
      security: [{ bearerAuth: [] }]
      responses:
        {
          "201": { description: Created },
          "403": { $ref: "#/components/responses/Error" },
        }
  /api/v1/admin/orders/{id}/status:
    patch:
      summary: Apply a validated order state transition
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/Id" }]
      responses:
        {
          "200": { description: Updated },
          "409": { $ref: "#/components/responses/Error" },
        }
  /api/v1/admin/integrations:
    get:
      summary: List masked integrations
      security: [{ bearerAuth: [] }]
      responses: { "200": { description: Integrations } }
    put:
      summary: Save encrypted provider credentials
      security: [{ bearerAuth: [] }]
      responses: { "200": { description: Saved } }
  /webhooks/{provider}:
    post:
      summary: Receive signed idempotent provider event
      parameters:
        - { in: path, name: provider, required: true, schema: { type: string } }
        - {
            in: header,
            name: X-Event-Id,
            required: true,
            schema: { type: string },
          }
        - {
            in: header,
            name: X-Webhook-Signature,
            required: true,
            schema: { type: string },
          }
      responses:
        {
          "202": { description: Accepted },
          "401": { $ref: "#/components/responses/Error" },
        }
components:
  securitySchemes:
    bearerAuth: { type: http, scheme: bearer, bearerFormat: JWT }
  parameters:
    Id:
      {
        in: path,
        name: id,
        required: true,
        schema: { type: string, format: uuid },
      }
  schemas:
    Credentials:
      type: object
      required: [email, password]
      properties:
        {
          email: { type: string, format: email },
          password: { type: string, minLength: 8, maxLength: 128 },
        }
    Register:
      allOf:
        - { $ref: "#/components/schemas/Credentials" }
        - {
            type: object,
            required: [name],
            properties:
              { name: { type: string, minLength: 2, maxLength: 100 } },
          }
    Checkout:
      type: object
      required: [lines, postalCode, contact, shippingAddress, paymentProvider]
      properties:
        lines:
          {
            type: array,
            minItems: 1,
            maxItems: 100,
            items:
              {
                type: object,
                required: [variantId, quantity],
                properties:
                  {
                    variantId: { type: string, format: uuid },
                    quantity: { type: integer, minimum: 1, maximum: 20 },
                  },
              },
          }
        postalCode: { type: string, pattern: "^[0-9]{6}$" }
        contact:
          type: object
          required: [name, email, phone]
          properties:
            name: { type: string }
            email: { type: string, format: email }
            phone: { type: string }
        shippingAddress:
          type: object
          required: [line1, city, state]
          properties:
            line1: { type: string }
            line2: { type: string }
            city: { type: string }
            state: { type: string }
            country: { type: string, default: IN }
        couponCode: { type: string }
        paymentProvider: { type: string }
        shippingService: { type: string, description: Service returned by the checkout quote }
    CheckoutQuote:
      type: object
      required: [lines, postalCode, paymentProvider]
      properties:
        lines:
          {
            type: array,
            minItems: 1,
            maxItems: 100,
            items:
              {
                type: object,
                required: [variantId, quantity],
                properties:
                  {
                    variantId: { type: string, format: uuid },
                    quantity: { type: integer, minimum: 1, maximum: 20 },
                  },
              },
          }
        postalCode: { type: string, pattern: "^[0-9]{6}$" }
        couponCode: { type: string }
        paymentProvider: { type: string }
        shippingService: { type: string, description: Optional delivery service to price }
  responses:
    Error:
      description: Standard error envelope
      content:
        {
          application/json:
            {
              schema:
                {
                  type: object,
                  properties:
                    {
                      success: { const: false },
                      error:
                        {
                          type: object,
                          properties:
                            {
                              code: { type: string },
                              message: { type: string },
                            },
                        },
                    },
                },
            },
        }
